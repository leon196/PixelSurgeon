
// Engine.filter = new PIXI.SimpleFilter();
// Engine.filter.frame[0] = frame.width;
// Engine.filter.frame[1] = frame.height;
// Engine.filter.pixelSize = 512;

// Engine.layer.filters = [Engine.filter];

 //    oscillation = Math.cos(tick * 0.01) * 0.5 + 0.5;
	// filter.pixelSize = Math.pow(2, Math.floor(oscillation * 4) + 6);